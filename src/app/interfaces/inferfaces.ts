export interface Componente {
    icon: string;
    name: string;
    redirectTo: string;
  }

export interface Hero{
  nombre: string,
  bio: string,
  img: string,
  aparicion: Date,
  casa:string
}